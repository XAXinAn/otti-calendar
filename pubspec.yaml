name: otti_calendar
description: "A Calendar."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  table_calendar: ^3.1.1
  intl: ^0.19.0
  lunar: ^1.3.3
  http: ^1.1.0
  icalendar_parser: ^1.1.1
  image_picker: ^1.1.2

  # Using a modern, maintained, and community-recommended package
  mobile_ocr: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
